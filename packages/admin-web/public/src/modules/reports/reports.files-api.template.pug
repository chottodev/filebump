h4 files-api-api
p количество запросов на files-api-api
.form-group
  label.control-label.col-md-1 Дата
  .col-md-3
    p.input-group
      input.form-control(
        uib-datepicker-popup="yyyy-MM-dd",
        ng-model="$ctrl.date",
        ng-change="$ctrl.refreshReport()",
        is-open="$ctrl.opened",
        datepicker-options="$ctrl.dateOptions"
      )
      span.input-group-btn
        button.btn.btn-default(
          type="button",
          ng-click="$ctrl.opened=!$ctrl.opened"
        )
          i.glyphicon.glyphicon-calendar

table.table.table-bordered
  thead
    tr
      th Endpoint
      th all
      th ok
      th fail
  tbody
    tr(ng-repeat="enpoint in $ctrl.report.endpoints")
      td {{ enpoint.endpoint }}
      td {{ enpoint.all }}
      td {{ enpoint.ok }}
      td {{ enpoint.fail }}
